{
  "name": "deacons-manage",
  "version": "0.1.0",
  "private": true,
  "description": "Church ushers & deacons management system - admin dashboard + usher scheduling with fair auto-rotation",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,frontend -c blue,green \"npm run dev -w server\" \"npm run dev -w frontend\"",
    "dev:server": "npm run dev -w server",
    "dev:frontend": "npm run dev -w frontend",
    "typecheck": "npm run typecheck -w server",
    "db:check": "npm run db:check -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run db:seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}